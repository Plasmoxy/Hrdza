mod draw;
mod snake;
mod game;

use piston_window::*;
use piston_window::types::Color;
use game::Game;
use draw::to_coord;

const BACK_COLOR: Color = [0.5, 0.5, 0.5, 1.0]; 

fn main() {
    let (W, H) = (20, 20);
    
    
}
